.footer-sell-btn
  - if user_signed_in?
    = link_to products_sell_path do
      %div 出品
      %i.icon-camera
        %i.fas.fa-camera.fa-3x
  - else
    = link_to new_user_session_path do
      %div 出品
      %i.icon-camera
        %i.fas.fa-camera.fa-3x
        