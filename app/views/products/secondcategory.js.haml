if($('#product_first_category_id').val() !== ""){
$('#second-select').html(
"
%div>
  .select-wrap= j(select :product, :second_category_id, options_from_collection_for_select(@secondcategory, :id, :second_category),{prompt: '---'}, {data: {remote: true, url: url_for(action: :thirdcategory)}})
"
);
}else{
$('#second-select').html(
""
);
}

$("#third-select").html("");
